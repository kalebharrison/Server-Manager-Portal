# Copy to dockhand.agent.env (gitignored) OR generate with:
#   ./scripts/dockhand-auth-cache.sh
#
# Deploy uses this file only — no 1Password on each deploy.
CURSOR_AGENT_USER=
CURSOR_AGENT_PASSWORD=
